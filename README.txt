#Starting MongoDB (MongoDB must be installed on PC, and class paths must be set)

1. In a bash shell (e.g. Git Bash): mongod, or run from MondoDB/bin/mongod.exe.


#Starting the web server

our signup form. In your browser, go to http://localhost:8080/signup and fill out you